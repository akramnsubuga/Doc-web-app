asrc/
├── pages/        # All main pages
├── components/   # Reusable UI components (buttons, inputs, navbar)
├── router/       # App routing
├── config/       # Firebase config
├── context/      # Global app state (auth, user info)
└── services/     # API calls, payment services
